<?xml version="1.0" encoding="utf-8"?>
<resources>
    
    <declare-styleable name="MyPressView">

        <!--背景颜色-->
        <attr name="mPressBackGoroundColor" format="color"/>
        <!--按下的背景颜色-->
        <attr name="mPressBackGoroundColorSelected" format="color"/>
        <!--圆角半径-->
        <attr name="mPressRadius" format="dimension"/>
        <!--内容是否默认居中-->
        <attr name="mPressContentCenter" format="boolean"/>
        <!--是否是圆形样式-->
        <attr name="mPressCircleView" format="boolean"/>
        
    </declare-styleable>

    <declare-styleable name="BottomBar">
        <attr name="bb_tabXmlResource" format="reference" />
        <attr name="bb_tabletMode" format="boolean" />
        <attr name="bb_behavior">
            <flag name="shifting" value="1" />
            <flag name="shy" value="2" />
            <flag name="underNavbar" value="4" />
            <flag name="iconsOnly" value="8" />
        </attr>
        <attr name="bb_longPressHintsEnabled" format="boolean" />
        <attr name="bb_inActiveTabAlpha" format="float" />
        <attr name="bb_activeTabAlpha" format="float" />
        <attr name="bb_inActiveTabColor" format="color" />
        <attr name="bb_activeTabColor" format="color" />
        <attr name="bb_badgeBackgroundColor" format="color" />
        <attr name="bb_badgesHideWhenActive" format="boolean" />
        <attr name="bb_titleTextAppearance" format="reference" />
        <attr name="bb_titleTypeFace" format="string" />
        <attr name="bb_showShadow" format="boolean" />
    </declare-styleable>


    <!-- 圆形进度条 -->
    <declare-styleable name="CircleProgressBar">
        <!-- 是否开启抗锯齿 -->
        <attr name="antiAlias" format="boolean" />
        <!-- 圆弧起始角度，3点钟方向为0，顺时针递增，小于0或大于360进行取余 -->
        <attr name="startAngle" format="float" />
        <!-- 圆弧度数 -->
        <attr name="sweepAngle" format="float" />
        <!-- 设置动画时间 -->
        <attr name="animTime" format="integer" />
        <!-- 绘制内容的数值 -->
        <attr name="maxValue" format="float" />
        <attr name="value" format="float" />
        <!-- 绘制内容的单位 -->
        <attr name="unit" format="string|reference" />
        <attr name="unitSize" format="dimension" />
        <attr name="unitColor" format="color|reference" />
        <!-- 绘制内容相应的提示语 -->
        <attr name="hint" format="string|reference" />
        <attr name="hintSize" format="dimension" />
        <attr name="hintColor" format="color|reference" />
        <!-- 精度，默认为0 -->
        <attr name="precision" format="integer" />
        <attr name="valueSize" format="dimension" />
        <attr name="valueColor" format="color|reference" />
        <!-- 圆弧颜色，设置多个可实现渐变 -->
        <attr name="arcColor1" format="color|reference" />
        <attr name="arcColor2" format="color|reference" />
        <attr name="arcColor3" format="color|reference" />
        <!-- 背景圆弧颜色，默认白色 -->
        <attr name="bgArcColor" format="color|reference" />
        <!-- 圆弧宽度 -->
        <attr name="arcWidth" format="dimension" />
        <!-- 圆弧颜色， -->
        <attr name="arcColors" format="color|reference" />
        <!-- 文字的偏移量。相对于圆半径而言，默认三分之一 -->
        <attr name="textOffsetPercentInRadius" format="float" />
        <!-- 背景圆弧宽度 -->
        <attr name="bgArcWidth" format="dimension" />
        <!--是否绘制文字-->
        <attr name="isDrawText" format="boolean"/>
    </declare-styleable>


</resources>
